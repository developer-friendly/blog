quiet: false

debug: false

insecure: false

timeout: 1m

exit-code: 1

cache:
  dir: .pre-commit-trivy-cache

scan:
  skip-dirs:
    - secrets

misconfiguration:
  terraform:
    exclude-downloaded-modules: false

severity:
  - HIGH
  - CRITICAL
